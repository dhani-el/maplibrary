

export interface SearchableMapProps{
    longitude:number,
    latitude:number,
    zoom:number,
    accessToken:string
}

export interface SearchBarProps{
    accessToken:string,
    country:string,
}